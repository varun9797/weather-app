{
  "name": "weather-app",
  "version": "1.0.0",
  "description": "A full-stack, containerized weather application using **React (Vite)** for the frontend, **Node.js** for the backend, **MongoDB** for data persistence, and a **WebSocket-based weather simulator** for real-time updates — all orchestrated using **Docker Compose**.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "docker-image": "cd node-server && npm run docker && cd ../weather-stream-simulator && npm run docker && cd ../react-client && npm run docker",
    "docker-compose": "docker-compose up --build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/varun9797/weather-app.git"
    
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/varun9797/weather-app/issues"
  },
  "homepage": "https://github.com/varun9797/weather-app#readme"
}
